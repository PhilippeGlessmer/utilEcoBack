{% extends 'base.html.twig' %}

{% block title %}Materiels{% endblock %}

{% block body %}
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
            <div class="container-fluid">
                <div class="row mb-2">
                    <div class="col-sm-6">
                        <h1>Process de test:  {{ step }}/ {{ nbproccess }}</h1>
                    </div>
                    <div class="col-sm-6">
                        <ol class="breadcrumb float-sm-right">
                            <li class="breadcrumb-item"><a href="{{  path('dashbord') }}">Accueil</a></li>
                            <li class="breadcrumb-item"><a href="">Process</a></li>
                            <li class="breadcrumb-item active">Proccess de test:  {{ step }}/ {{ nbproccess }}</li>
                        </ol>
                    </div>
                </div>
            </div><!-- /.container-fluid -->
        </section>

        <!-- Main content -->
        <section class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <!--<div class="card">
                            <div class="card-header">
                                <h3 class="card-title">Materiels</h3>
                            </div>

                            <div class="card-body">
                                <table class="table">
                                    <tbody>
                                    <tr>
                                        <th>Id</th>
                                        <td>{{ materiel.id }}</td>
                                    </tr>
                                    {% if materiel.contactusername %}
                                        <tr>
                                            <th>Contact</th>
                                            <td>{{ materiel.contactusername }}</td>
                                        </tr>
                                        <tr>
                                            <th>Contact</th>
                                            <td>{{ materiel.contactemail }}</td>
                                        </tr>
                                    {% endif %}

                                    {% if materiel.distributeurs %}
                                        <tr>
                                            <th>Distributeur</th>
                                            <td>{{ materiel.distributeurs.libelle }}</td>
                                        </tr>
                                    {% endif %}
                                    {% if materiel.lots %}
                                        <tr>
                                            <th>Fournisseur</th>
                                            <td>{{ materiel.lots.fournisseur.libelle }}</td>
                                        </tr>
                                        <tr>
                                            <th>Lots</th>
                                            <td>{{ materiel.lots.id }}</td>
                                        </tr>

                                    {% endif %}
                                    <tr>
                                        <th>Image </th>
                                        <td><img src="{{ vich_uploader_asset(materiel.modele, 'imageFile') }}"  alt="" class="w-25"></td>
                                    </tr>
                                    <tr>
                                        <th>Notice </th>
                                        <td><a href="{{ vich_uploader_asset(materiel.modele, 'noticeFile') }}" target="_blank">{{ materiel.modele.notice }}</a></td>
                                    </tr>
                                    <tr>
                                        <th>Pilote </th>
                                        <td><a href="{{ vich_uploader_asset(materiel.modele, 'piloteFile') }}" target="_blank">{{ materiel.modele.pilote }}</a></td>
                                    </tr>
                                    <tr>
                                        <th>Type</th>
                                        <td>{{ materiel.modele.type.libelle }}</td>
                                    </tr>
                                    <tr>
                                        <th>Marque</th>
                                        <td>{{ materiel.modele.marque.libelle }}</td>
                                    </tr>
                                    <tr>
                                        <th>Modele</th>
                                        <td>{{ materiel.modele.libelle }}</td>
                                    </tr>
                                    <tr>
                                        <th>NSerieFabricant</th>
                                        <td>{{ materiel.nSerieFabricant }}</td>
                                    </tr>
                                    {% if materiel.nSerieFournisseur %}
                                    <tr>
                                        <th>NSerieFournisseur</th>
                                        <td>{{ materiel.nSerieFournisseur }}</td>
                                    </tr>
                                    {% endif %}
                                    </tbody>
                                </table>

                            </div>
                        </div>-->
                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title">Les procédures de tests d'appraillage:  | <span class="badge badge-info"> {{ step }}/ {{ nbproccess }}</span></h3>
                            </div>
                            <!-- /.card-header -->
                            <div class="card-body">
                                {% for procedure in process %}
                                <div class="row">
                                    <div class="col-9">

                                            <span style="font-size: 4em">{{ procedure.action }}</span>

                                    </div>
                                    <div class="col-3">
                                        <img src="{{ vich_uploader_asset(procedure, 'imageFile') }}"  alt="" class="w-100 img-fluid">
                                    </div>
                                </div>
                                {% endfor %}
                            </div>
                            {{ form_start(form) }}



                                <div class="card-body">
                                    <div class="row">
                                        <div class="col-6">
                                            <label class="btn btn-success w-100 active row">
                                                {{ form_row(form.Reponse[1]) }} <i class="fas fa-forward"></i>
                                            </label>
                                        </div>
                                        <div class="col-6">
                                            <label class="btn btn-danger w-100">
                                                {{ form_row(form.Reponse[0]) }} <i class="fas fa-eject"></i>
                                            </label>
                                        </div>
                                    </div>
                                </div>
                                <div class="card-footer">
                                    {{ form_row(form.save) }}
                                </div>

                            {{ form_end(form) }}



                        </div>

                    </div>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
