{% extends 'base.html.twig' %}
{% block title %}Particuliers{% endblock %}

{% block body %}
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
            <div class="container-fluid">
                <div class="row mb-2">
                    <div class="col-sm-6">
                        <h1>Particuliers:</h1>
                    </div>
                    <div class="col-sm-6">
                        <ol class="breadcrumb float-sm-right">
                            <li class="breadcrumb-item"><a href="{{  path('dashbord') }}">Accueil</a></li>
                            <li class="breadcrumb-item active">Particuliers</li>
                        </ol>
                    </div>
                </div>
            </div><!-- /.container-fluid -->
        </section>

        <!-- Main content -->
        <section class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title">Tableau des particuliers</h3>
                                <a href="{{ path('pointscollects_new') }}" class="btn btn-outline-success float-right">Entrer un nouveau point collect</a>
                            </div>
                            <!-- /.card-header -->
                            <div class="card-body">
                                <table id="example1" class="table table-bordered table-striped">
                                    <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Libelle</th>
                                        <th>Adresse</th>
                                        <th>CodePostal</th>
                                        <th>Ville</th>
                                        <th>TelFixe</th>
                                        <th>TelPortable</th>
                                        <th>CoordLatLng</th>
                                        <th>actions</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for pointscollect in pointscollects %}
                                        <tr>
                                            <td>{{ pointscollect.id }}</td>
                                            <td>{{ pointscollect.libelle }}</td>
                                            <td>{{ pointscollect.adresse }}</td>
                                            <td>{{ pointscollect.codePostal }}</td>
                                            <td>{{ pointscollect.ville }}</td>
                                            <td>{{ pointscollect.telFixe }}</td>
                                            <td>{{ pointscollect.telPortable }}</td>
                                            <td>{{ pointscollect.coordLatLng }}</td>
                                            <td>
                                                <a href="{{ path('pointscollects_show', {'id': pointscollect.id}) }}" class="btn btn-success">Voir</a>
                                                <a href="{{ path('pointscollects_edit', {'id': pointscollect.id}) }}" class="btn btn-warning">Modif.</a>
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                    <tfoot>
                                    <tr>
                                        <th>Id</th>
                                        <th>Libelle</th>
                                        <th>Adresse</th>
                                        <th>CodePostal</th>
                                        <th>Ville</th>
                                        <th>TelFixe</th>
                                        <th>TelPortable</th>
                                        <th>CoordLatLng</th>
                                        <th>actions</th>
                                    </tr>
                                    </tfoot>
                                </table>
                            </div>
                            <!-- /.card-body -->
                        </div>
                        <!-- /.card -->
                    </div>
                    <!-- /.col -->
                </div>
                <!-- /.row -->
            </div>
            <!-- /.container-fluid -->
        </section>
        <!-- /.content -->
    </div>
{% endblock %}
{% block javascripts %}

    <!-- DataTables -->
    <script src="{{ asset('plugins/datatables/jquery.dataTables.min.js') }}"></script>
    <script src="{{ asset('plugins/datatables-bs4/js/dataTables.bootstrap4.min.js') }}"></script>
    <script src="{{ asset('plugins/datatables-responsive/js/dataTables.responsive.min.js') }}"></script>
    <script src="{{ asset('plugins/datatables-responsive/js/responsive.bootstrap4.min.js') }}"></script>
    <!-- AdminLTE App -->
    <script src="{{ asset('js/adminlte.min.js') }}"></script>
    <!-- page script -->
    <script>
        $(function () {
            $("#example1").DataTable({
                "responsive": true,
                "autoWidth": false,
            });
            $('#example2').DataTable({
                "paging": true,
                "lengthChange": false,
                "searching": false,
                "ordering": true,
                "info": true,
                "autoWidth": false,
                "responsive": true,
            });
        });
    </script>
{% endblock %}
